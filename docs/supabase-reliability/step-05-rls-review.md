# Step 5: current RLS and role-access verification

## Inspection and controlled plan

Step 4's final release `3cdb4c116a315b16c7fe0b05dc9ab06a13142c0a` was pushed, its exact Vercel deployment AoDWMKiJm548TNJ5Kq8xSm2G8Npw succeeded, and production health/readiness passed at 00:53 UTC on 2026-09-10. Final read-only duplicate checks at 00:54 UTC found no duplicate posted dates, active check-ins, photo paths, case-insensitive dancer links, active primary/gallery positions, shared avatar/gallery paths or moderation-error rows. All 145 unique indexes are valid and ready. Lifecycle, external-delivery atomicity and disposable multi-connection testing remain explicitly separate follow-ups.

Fresh read-only catalog capture at 00:55:22 UTC contains seventy-nine public base tables, two public views and 140 public policies; every base table retains RLS. Since the initial architecture snapshot, ten restrictive active-account preference policies were added and administrator audit history was changed to authenticated-admin read access with server-only writes. These are independently delivered protections, not new defects to reapply. The public browser-executable helpers/RPCs and their pinned execution boundaries remain visible in the catalog; their full privileged-operation review also belongs to Step 13.

The existing policy test fixture uses the initial audit catalog plus a later preference migration. Keep those historical regression cases. Add an optional current-catalog input, verify its helper definitions and column grants against production metadata, and exercise the current policies independently with synthetic rows and explicit customer, dancer, venue, admin, anonymous and service identities. Map every public relation's CRUD grants and policy intent. Test unrelated-user reads/writes, protected account/approval fields, public visibility, owner preferences, admin boundaries, and server access. Never execute production writes or policy-removal probes to demonstrate a failure.

Only change a production policy if a concrete failing boundary is demonstrated and its intended behavior is established. Avoid merging owner/admin policies or restricting intentional self-service reads/deletion merely to reduce advisor warnings. Column grants and views are part of the access boundary; RLS enabled alone is not a successful test. This fixture tests PostgreSQL policy semantics with synthetic rows, not complete Supabase Auth/Storage/Realtimes services or deferred historical schema replay. Record any remaining untested external behavior honestly.

Run focused tests, the complete suite, lint, production build, standalone TypeScript and live read-only checks; then commit/push the bounded Step 5 change and verify its exact Vercel deployment and production health before Step 6. The already applied active-photo-position migration is frozen using its verified checksum as part of this delivery record; it must not be edited or reapplied.

## Findings and implementation

The old runtime fixture did not include the current administrator audit policy, the exact current policy-helper bodies, or the latest column metadata. The new metadata-only snapshot and optional fixture inputs close that regression gap without replacing the historical cases. The [access matrix](step-05-access-matrix.md) covers all 81 public relations. The fixture verifies every captured policy, base-table CRUD grant, explicit column SELECT grant, view security option, and the two policy-helper bodies. It incorporates three additive column changes; no production columns are removed.

The current policy tests create synthetic private records in all 79 base tables and use positive service-role reads before 1,264 unrelated-role/command checks. Additional cases exercise customer preferences, private owner reads, dancer/venue ownership, active administrator audit reads, blocked browser audit writes, disabled administrators, forged metadata, protected account and media approval fields, restricted private columns and analytics, and published/hidden profile visibility. Only permission errors or empty row results count as denials; unexpected SQL errors fail the test.

No tested current-policy boundary required changing production RLS. Existing overlapping public/owner/admin SELECT policies are intentional alternatives, while the ten active-account policies are restrictive and combine with ownership checks. Broad table SELECT grants do not bypass these row policies. Administrator audit writes and private recovery records remain server-only. The 82 explicit column grants cover 41 public columns for each of anonymous and authenticated roles; private legal names remain inaccessible through those browser grants. Service-role access is intentionally privileged and requires application authorization, which is a separate server-operation boundary.

This is a policy-semantic fixture, not a complete schema replay: most non-preference constraints, triggers, hosted JWT issuance, Storage and Realtime are absent. Queued PGlite execution does not demonstrate independent database-connection concurrency. Full hosted cross-account sessions remain deferred with the disposable project. Storage policy/byte visibility and all callable RPCs still require Steps 11 and 13; no claim of zero system-wide RLS bypasses follows from these tests. No production account, private row, policy or storage object was changed for testing.


Final Step 5 validation on `b8f3f254ddea427252cef528cffb7b118f5330ed` passed all 3,614 tests without failures, skips or cancellations, full lint, the production build and standalone TypeScript. Ninety new current-catalog cases and ninety-four historical policy cases passed independently. The complete suite used three workers after the normal generators, and postbuild skipped layout-review population. All thirty live readiness checks passed. Read-only metadata rechecks at 01:12:37–42 UTC on 2026-09-10 matched all 81 relation configurations, 140 policies, 82 column grants, both policy helper definitions and all three role configurations exactly. The existing applied position migration is frozen; no new migration, policy, production account or data write is included. Exact push, Vercel success and post-deployment health remain the final release gates.


The first push encountered concurrent webhook security commit `9de701e9`, which was preserved by rebasing. Its new SQL-fixture fingerprint test exposed Windows checkout line endings: the loader now explicitly normalizes CRLF to LF, as its existing checksum and documentation require. All 53 webhook cases then passed independently; the expected SQL checksum and production function were not changed. The complete integrated checks are being repeated before publication.

Final validation after integration passed all 3,667 tests with no failures, skips or cancellations, full lint, production build, standalone TypeScript and all thirty live readiness checks. This supersedes the earlier 3,614-test release result; the Windows SQL fixture loader fix is the only change to the incoming webhook tests. Exact deployment verification remains required.
