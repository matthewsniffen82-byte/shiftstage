# Step 4: duplicate pending content reviews

## Inspection and plan

This work starts after `28f9cd24e31a82ba25d8b8fb3b4e2539cb7db746` was pushed and its exact Vercel deployment 6Qt6osvyM8KSNMUaxXeD2c7HB7gP succeeded. Both health routes passed at 23:57:53-54 UTC on 2026-09-09, both deletion endpoints and protected profile/upload routes denied unauthenticated requests, and all thirty readiness checks passed. That caller release passed all 3,389 tests, lint, build and standalone TypeScript.

The profile-save handler checks which photo/social-link reviews are pending before inserting missing rows. Two requests can both observe no review and insert duplicates. There is no corresponding unique database key. At 23:59:50 UTC, read-only production inspection found twenty review rows, seven pending content reviews, zero duplicate pending content groups and one repeated completed-history group. Preserve all history. No stored photos currently have pending status; active socials number at most five per profile. Existing RLS and the independently hardened browser privileges remain enabled.

1. Add one partial unique index on dancer/review type for pending `photo:` and `social_link:` entries only. Keep completed/rejected history and other review types outside this guard. If a fresh duplicate exists before creation, abort; do not delete or merge records to force the index.
2. Add an unused service-only enqueue function with invoker security, empty search path, bounded input and lock timeout, active owner authorization, a profile lock and ordered locks on the supplied owned targets. Insert eligible pending-photo or active-social reviews with `ON CONFLICT DO NOTHING` and return the inserted count. Reject foreign/deleted targets and preserve every existing review decision and note.
3. Validate repeat/overlapping batches, direct duplicate writes, completed-history preservation, different profiles/targets, target/account/role rejection and transactional failure using synthetic PostgreSQL data. Queued embedded tests do not substitute for multi-connection Supabase testing.
4. Validate and deliver this database foundation first, with a bounded migration/ledger transaction, preservation fingerprints and exact deployment/health verification. Existing valid inserts remain compatible; duplicate pending inserts fail instead of storing duplicate rows. The old check/insert caller remains a known retry/error limitation until the next release.
5. After that verified release, switch both profile-save queues to the new function, preserving normal profile fields and explicit submission behavior; then repeat all validation, commit, push and exact deployment checks. Freeze applied SQL rather than editing it.

No review history, account, photo, social link, policy or existing index is rewritten or deleted. Keep the guard and function during application rollback. Removing the unique guard later would reintroduce duplicate risk and requires an explicit reviewed migration. Profile/social-field saves and queue insertion still span separate requests, and administrator decision/history atomicity remains part of Step 9; this change alone must not be reported as making those whole workflows transactional. Pending-review retirement after content deletion also needs the lifecycle review.
