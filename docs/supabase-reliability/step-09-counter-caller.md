# Step 9: activate atomic counter-notice submission

Prerequisite `52d6a240bf95d2b805e38f23c18aad14cefa1fb1` is pushed with matching main references and [successful exact Vercel deployment](https://vercel.com/ai-movie-jobs/shiftstage/2RYRD6oKZkqNH18BkhmCn2WgM1hY). The additive transaction preserved 29 accounts, 34 videos, zero cases/counters/strikes, 135 catalog column rows, 47 constraints, nineteen indexes, eleven policies, two triggers, five relation access records, 117 existing functions and 99 prior migration records. At 05:16:15 UTC the function/source MD5s matched `95d49e30ad70d6c10d9f192582e09eb8` and `87e724067cb27c84328e168dbd981a18`; takedown/restoration functions, RLS and server-only access were preserved. Health, uploader/admin/cron authorization denials and thirty readiness checks passed at 05:18:27–30 UTC. No production notices or emails were created.

## Controlled caller plan

Freeze the applied migration. Replace separate notice insert, case update and compensating deletion with one checked RPC. Keep all legal input validation and explicit confirmations. Validate the returned case/notice identity, status, timestamps and replay flag; propagate useful sanitized errors and never fall back to separate writes. Use the database's stored receipt dates throughout email content and public responses. Same-case identical retries return the original receipt, skip email/optional notification work and preserve later administrative state. The form must use the returned case state and receipt date rather than resetting them from the browser clock.

Keep forwarding acknowledgment checks and best-effort uploader notification. An unexpected external-mail exception after the database commit must return a received-but-unconfirmed delivery result, without deleting records or resending within the request. External provider ambiguity and scheduled forwarding overlap still need their own durable dispatch/idempotency review; this release does not claim exactly-once email delivery. No production email, case action, account mutation or migration reapplication is needed for verification.

Exercise actual application helpers/endpoints against native SQL: normal submit, lost response/retry, changed payload, terminal replay, SQL rollback, malformed results, cross-user access, delivery exceptions and retained notification/forwarding behavior. Preserve previous delivery regression coverage. Run full suite, lint, build, standalone TypeScript, migration guard, read-only RPC schema/dependency and readiness checks. Commit/push only this caller release and verify exact Vercel/production health before proceeding. Roll back application usage if needed; keep the additive database function.

## Validation before delivery

All 87 focused checks passed, including forty new tests of the actual helper, endpoint and form handler against native PostgreSQL. The prior library fails 36 of those regressions. Existing forwarding acknowledgment and private-error tests remain covered. Full validation passed all 4,424 tests, lint, production build, standalone TypeScript, migration-history guard and thirty live readiness checks; postbuild skipped layout-review population. Read-only checks at 05:26:41–43 UTC confirm RPC API exposure, frozen function/ledger hashes, preserved takedown/restoration helpers, server-only access, retained RLS and zero production cases/counters/strikes. No migration was reapplied or external email sent. Exact push, Vercel success and deployed health remain required.
