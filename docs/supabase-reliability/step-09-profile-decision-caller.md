# Step 9: connect atomic whole-profile reviews

Foundation `7a539275d90e9d506c131c60761f4b725f52f3e3` is pushed with matching main references and successful [exact Vercel deployment](https://vercel.com/ai-movie-jobs/shiftstage/H886FzLfLbQj9uajWQG9xVP6FN6B). Exact committed SQL was applied, preserving all existing records, six relation access records, 100 catalog columns, twenty constraints, fourteen indexes, seven policies, two triggers, 120 existing functions and 102 prior migration entries. Read-only verification at 07:07:44 UTC confirmed hashes and service-only execution. Health, protected routes, RPC schema exposure and thirty readiness checks passed at 07:09:45–49 UTC on September 10, 2026.

## Inspected caller plan

Keep the applied SQL frozen. Add the raw loaded profile version to authorized administrator responses and submit that version with whole-profile decisions. Include the exact fifteen snapshot fields required by the transaction; retain native timestamp precision and missing-field detection. Legacy pages must refresh before deciding. Do not reconstruct the expected version from a new server read.

Replace separate publication/history/audit writes with one checked RPC. Validate identity, decision, resulting private state, review/audit identifiers, timestamps and returned version. Preserve API acceptance as private/pending and UI rejection behavior. No missing-function fallback, automatic retry or compensating delete is allowed. Old versions conflict after confirmed or uncertain commits; a later deliberate action starts from a refreshed version.

After a confirmed decision, issue-summary lookup, notification storage and delivery are optional follow-up work. Catch their failures, log sanitized metadata and return the confirmed review with an explicit notification warning. The UI must validate the returned decision before removing the dancer from the pending list or displaying success. Include the warning in the persistent confirmation.

## Verification and recovery

Test the actual mapper, endpoint, helper and UI handler against the native transaction, including changed versions, legacy requests, permissions, lost committed responses, malformed acknowledgments and optional follow-up failures. Keep existing publication/security tests. Run the full suite, lint, build, standalone TypeScript, migration guard, live readiness and read-only frozen dependency checks. Commit/push only this caller change; verify exact Vercel success and deployed health before continuing.

No production profile decision or notification is used as a test. The function remains installed during application rollback. External delivery is still best effort, and restoring the old application would restore its separate-write limitations; prefer a forward fix if a release issue appears.

## Validation before delivery

All 171 focused checks passed, including 47 new actual mapper/endpoint/helper/UI cases backed by PostgreSQL. The full suite passed all 4,729 tests after updating one old source assertion to expect the atomic caller; native tests retain the existing private-publication and suspension checks. Lint, production build, standalone TypeScript, migration guard and thirty live readiness checks passed. Read-only dependency checks at 07:16:58–07:17:00 UTC retained exact function, nested publication, queue, social-save and ledger hashes, access settings and aggregate data counts. No migration was reapplied or production decision/notification created. Exact push, Vercel success and deployed health remain required.
