# Step 9: counter-notice submission consistency

NFC caller `30f41526a4ccb17b1792c6304df29b1e62f89ddd` is pushed with matching local/remote main references and [successful exact Vercel deployment](https://vercel.com/ai-movie-jobs/shiftstage/AacfmKbF66zEbhrhfqetqb4BahZw). Production health, protected NFC/account/finance denials, RPC schema exposure and thirty readiness checks passed at 05:04:27–31 UTC on 2026-09-10. No production message or migration reapplication occurred.

## Inspected risk and controlled plan

`submitDmcaCounterNotice` inserts a notice, then separately changes the case from disabled to countered. Its error compensation deletes the notice even if the case transition committed but its response was lost. The existing unique case constraint prevents two notice rows but does not make the two operations atomic. Client-side double-submit protection does not repair an interrupted transaction. Existing forwarding acknowledgment improvements remain intact; external email dispatch requires a separate recovery boundary.

First add an unused service-only security-invoker `submit_dmca_counter_notice_safely(uuid,uuid,jsonb)` function. Validate required fields and all four explicit confirmations, lock the uploader's case, and preserve the current rule that only a disabled case accepts a new counter-notice. Do not add an active-account requirement: existing authenticated uploader appeals must remain available even when an account was disabled. Never alter strikes, account suspension, media visibility, claimant information or administrative decisions.

The case's existing unique notice relationship is the retry identity. Identical normalized notice details and uploader return the original receipt without rewriting status, legal details, timestamps or sending anything. Different details conflict; incomplete pre-existing relationships fail for review rather than being overwritten or deleted. A missing/wrong-uploader case is denied. Insert the notice and transition the case in one transaction; any failure rolls both back. Keep external email and optional notifications outside this unused function.

Use one database timestamp after acquiring the case lock. Preserve the current ten/fourteen weekdays calculation in UTC, excluding weekends exactly as the existing `addBusinessDays` helper does; do not introduce different legal/calendar policy. Return stored receipt/timing fields and the claimant contact only to the privileged server caller. The later caller release must validate its result, skip repeated external delivery on replay and use those stored dates. Current application behavior remains until the migration is deployed and verified.

## Evidence and delivery gates

The 05:06:35 UTC read-only inspection captured 59 columns, 26 constraints, no user triggers, RLS on all three case/counter/strike tables and valid case uniqueness. Production counts are zero cases, counters and strikes, 29 accounts and 34 videos. Existing service-only `apply_dmca_takedown` and `restore_dmca_case` MD5s are `bd41d4fde042aae77067fbee550742f7` and `7e44a85cc2c03473d67541300ee69936`; preserve them unchanged. Only schema metadata and aggregate counts are saved in the fixture.

Test actual SQL with captured columns/constraints and synthetic references. Cover uploader access, disabled-account appeals, incomplete confirmations, invalid types/lengths/email, unique retries, changed payloads, terminal decisions, missing/broken prior linkage, cross-timezone date consistency, concurrent queued requests and injected insert/update failures. Shared-connection PGlite does not replace deferred hosted multi-connection testing. Use a guarded, committed additive migration with deadlines, ledger/name/schema/trigger checks and before/after data/access/catalog/function preservation. Full suite, lint, production build, standalone TypeScript, readiness, push, exact Vercel success and production health must pass before the caller changes. Do not send real notices/emails or mutate production cases as tests. Leave an unused function in place if a forward correction is needed; never replay history or delete case data.

## Validation before delivery

All 82 new PostgreSQL cases and all 4,384 automated tests passed. Full lint, production build, standalone TypeScript, migration-history guard and thirty live readiness checks passed; postbuild skipped layout-review population. The fixture uses all captured target columns/constraints with synthetic account/media projections; local PostgreSQL 18 and hosted PostgreSQL 17 catalog differences are handled by checking column nullability separately from primary/foreign/check constraints.

The local guarded deployment passes transaction, repeated-version and schema-drift rejection tests. It checks exact target columns/constraints, rejects unexpected target triggers, and fingerprints the five related tables, their access/catalog definitions, all existing public functions and previous ledger entries before/after the additive change. Expected migration SHA-256 is `b66d08f060c658682311a5d12a6414779c8f59de68a1e92d4ccb0fd5205be7e4`; function MD5 is `95d49e30ad70d6c10d9f192582e09eb8`. Exact push, committed SQL application, read-only hash/permission/data verification, Vercel success and deployed health remain required before changing the caller.
