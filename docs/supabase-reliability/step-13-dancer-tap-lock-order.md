# Step 13 — serialize dancer NFC enrollment entry points

The freshly inspected registration function locks the dressing-room tag before its enrollment upsert; deferred dashboard completion updates/locks enrollment rows before calling affiliation approval, which locks that tag. For the same dancer, simultaneous fresh tap and dashboard completion can therefore form a tag/enrollment wait cycle. PostgreSQL aborts a deadlock victim, so the identified risk is a failed activation/dashboard request, not evidence of committed partial data or a live incident.

The only application registration caller uses the previously deployed atomic tap wrapper. Deferred completion is called by the authenticated active-dancer dashboard GET, as a separate RPC before its reads. Neither caller holds a database row lock across the HTTP call. The public-schema function catalog shows only the wrapper calling registration and no SQL caller of deferred completion. The two inspected functions remain service-only definers. All approval and presence business rules stay in their current downstream functions.

Plan: acquire the same transaction-scoped advisory lock for the dancer user ID at the beginning of registration and deferred completion, before either can lock tags or enrollment rows. Keep it distinct from existing per-profile presence and venue-affiliation locks. Serialize only requests for the same dancer and release automatically on commit/rollback. Preserve signatures, ownership, access, search paths, timestamps, pending expiry/selection, authorization, eligibility, all writes and the fresh-tap requirement for Working Now. Do not rewrite historical migrations. Test native held-lock identity/lifetime, both call paths, existing admission/approval/presence behavior and failure rollback. Hosted independent-session concurrency is still deferred and must not be claimed passed.

Current native metadata: eleven target tables, 177 columns, 77 constraints, 49 indexes and twelve triggers. The four gallery triggers added since the initial tap fixture must also be installed; retain their actual definitions and explicitly model supporting history/retirement tables. No production profile, tap, affiliation, schedule, notification or stored file is used as a mutable test fixture. The existing four enrollment records are not rewritten by deployment.

Use a narrow migration for these two function bodies, guarded by the freshly captured old definitions and service-only execution rights. Verify other function and schema metadata, all relevant row fingerprints and prior migration ledger entries remain unchanged. Rehearse transactional failure and repeat rejection, then run the full automated suite, lint, production build, standalone TypeScript and read-only readiness before commit/push/deployment. Verify exact Vercel success and production health before proceeding. Rollback is restoration of the two captured old definitions through a reviewed forward change, which reintroduces the wait-cycle risk; preserve all data and ledger history.

The guarded migration rehearsal passed repeat rejection, definition/access/RLS drift rejection and rollback of injected row/access changes. The source SHA-256 is `6001c96c8a7caaa5a828e2581f9ab6a79716d67559298ea7d73ce8e8fc8fa031`; source MD5 is `5c4fe70307f6dcf3027be2a513881400`. Expected fingerprints: `finalize_pending_dancer_nfc_enrollment(uuid,uuid,jsonb)` → `f382dae05f592ee9b34cdad4e6c4afe7`; `register_dancer_nfc_enrollment(uuid,uuid,uuid,jsonb)` → `dd876469ac66d123922e9da3fd4e33b3`. All 25 focused tests pass, ten fail against the old functions, and thirty readiness checks plus the migration guard passed. Full-suite/lint/build/standalone TypeScript and delivery/health gates remain required.

After integrating the separately delivered webhook review at `060b2976d3e7856efb2cc0c9ce3c64c0732fb919`, the complete suite passed all 6,068 tests with zero failures or skips. Lint, production build, standalone TypeScript after the build, migration guard and thirty read-only readiness checks passed. Fresh production metadata matches the native fixture's eleven tables, 177 columns, 77 constraints, 49 index definitions and twelve triggers. Production remains at 116 ledger entries before this correction. Exact committed-source application, push, Vercel success and independent preservation/health verification remain required; no production tap or dashboard finalization was invoked as a test.

A concurrent security release at `fa3b140543c4c5092be431f43b1c3a37793123d2` made the first push fail safely. The unpublished correction was rebased without changing its migration or tests. Combined validation then passed all 6,104 tests, lint, production build, standalone TypeScript, the build's migration check and thirty read-only readiness checks. The committed-source guarded transaction rehearsal also passed. Exact push, migration application and deployed health/preservation gates remain required.
