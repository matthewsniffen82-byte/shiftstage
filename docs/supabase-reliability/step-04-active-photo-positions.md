# Step 4: unique active photo positions

## Inspection and plan

This work begins after caller release `ac9696c18296871eb48999f450de00d5c33a5ae7` was pushed and its exact Vercel deployment Df9TP35uNE1ihYh6nnXpCv8QNVjL succeeded. Both health endpoints returned 200/ok at 00:39:47-48 UTC on 2026-09-10; four protected requests returned 401 and all thirty readiness checks passed. All 3,495 tests, lint, build and standalone TypeScript passed before delivery.

The publisher and primary selector serialize their own changes, but the stored-photo administrator review handler can change a rejected photo back to approved directly. If another active photo has since taken its primary flag or gallery position, that update can create duplicate active positions. The photo table currently has only its primary-key index. Read-only inspection at 00:41:24 UTC found seventy-two photos, no duplicate active primary or positive gallery positions, no null position/status fields and four legacy non-primary photos at position zero. Keep those legacy rows unchanged.

1. Add two partial unique indexes: one active primary per dancer, and one active non-primary photo per positive gallery position per dancer. Active means pending or approved. Exclude rejected history and legacy gallery position zero. Fail the migration if conflicting active rows appear; do not delete, renumber or merge them.
2. Map only these two index conflicts in the administrator photo-review handler to a useful 409 response before summary, review history, audit log or notifications run. Preserve unrelated errors. Do not automatically overwrite the current photo or move an older rejected photo to a guessed position. The gallery publication gateway already maps uniqueness conflicts to 409.
3. Use native PostgreSQL tests for duplicate inserts, status changes, historical/legacy preservation, independent dancers/positions, failed-migration rollback, and compatibility with the deployed publisher and primary selector. Exercise the actual administrator handler and verify that conflicting approval stops all later writes. Synthetic fixtures do not replace a disposable multi-connection Supabase environment.
4. Run the complete suite, lint, build, standalone TypeScript and live readiness checks; commit/push only this change. Apply the exact committed migration through a bounded transaction that preserves all existing photo/profile/review rows, prior ledger entries, grants and policies. Verify index definitions and production health plus the exact Vercel deployment before continuing.

No existing row, index, grant, policy or applied migration changes. Keep the uniqueness guards during application rollback. Removing either guard would reopen the integrity risk and requires a separately reviewed migration. Administrator review decisions still span multiple database requests and currently update earlier review history; their transaction/content-version design belongs to Step 9. This release prevents duplicate active positions without claiming that whole workflow is atomic.
