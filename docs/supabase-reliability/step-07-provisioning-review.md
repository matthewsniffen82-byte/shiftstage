# Step 7: recoverable account and profile provisioning

## Inspection and controlled plan

Step 6 was pushed as `ca0e2c7d45582a04d55657a3ec30fcf82a98e0e7`; its exact Vercel deployment HnwBNcoWWo6LEWHXAPZBMzCJU6jQ succeeded. Production health/readiness, safe authentication denials, the invalid recovery state and updated reset-client code passed at 01:38:11–15 UTC on 2026-09-10. Local and remote main references matched. No email, credential or production data mutation was used for verification.

The repository already contains an atomic, service-only `provision_app_account_safely` function. It inserts missing records with conflict protection, locks the account role, preserves existing profiles and starts dancers privately. Signup uses it and a callback repairs a missing account, but an existing account with a missing child profile is not normally repaired. Login can return an account that still cannot use its dashboard. Earlier aggregate inspection found one active dancer account without its profile; no mass production repair is planned.

First verify the current function, Auth bootstrap and relevant triggers/constraints with read-only metadata and aggregate counts. Add native PostgreSQL failure/idempotency coverage against the actual captured provisioner. Connect verified account entry to a narrow repair helper: preserve the database role/state, query the role's own profile, call the existing provisioner only when that profile is missing, and verify the result. Missing public accounts may be reconstructed only from a provider-verified identity and public customer/dancer role hints; never infer admin or venue authority from user metadata. Disabled/deleted accounts and existing profile data must remain unchanged.

Authentication/callback/account-entry callers must never treat a failed query as a missing record, report an unacknowledged setup as successful, retry an uncertain write automatically, or delete the Auth user as compensation. Preserve the ordinary signup and private setup paths. Auth-trigger failure rollback, missing application accounts, missing child profiles, repeated attempts, preserved established profile/role, denied browser RPC calls and infrastructure failures require tests. Account deletion, venue invitation compensation and broader transaction/lifecycle work remain in Steps 9 and 17.

Run focused and complete tests, lint, production build, standalone TypeScript and live read-only checks. Commit/push only the completed step, verify exact Vercel success and production health, and then begin Step 8. Hosted account creation and historical replay remain deferred; synthetic database execution is not a hosted Auth signup test.

## Confirmed database issue and migration safety

Fresh captures at 01:42:13 and 01:45:50 UTC confirmed zero Auth identities without application accounts, zero active customers without profiles, one active dancer without a profile, and no duplicate dancer owners. No IDs or user records were exported. The existing provisioner has fingerprint `f55c6f2411fe1e22da7fe068d690e70d`; its exact body and the Auth/bootstrap/link helpers are preserved as metadata in the native test fixture.

Native PostgreSQL reproduced another provisioning failure: a profile created by the repair RPC can acquire a stage-name link, reserving its old generated link. A repeated call attempts INSERT before its ON CONFLICT clause is considered. The existing BEFORE INSERT trigger rejects the new candidate row because the old link belongs to the established profile. This raises 23505 instead of acknowledging the unchanged profile.

`20260910014900_preserve_existing_account_profiles.sql` replaces only the existing function with the same arguments and return type. Under the existing account row lock, it skips child insertion when that child already exists and does not recreate children for disabled/deleted accounts. Missing active profiles still use the existing unique constraints, private defaults and insert-only behavior. The function retains service-only execution, fully qualifies application objects, pins an empty search path and bounds lock waits to three seconds. No table, constraint, index, trigger, RLS policy, link, approval or production row is removed or rewritten. The new callers are compatible with the prior signature and do not use the legacy multi-write fallback.

Deployment will apply only the exact committed replacement and its ledger entry in a bounded transaction, preserving application row fingerprints, Auth identity counts, relation grants/RLS, triggers and every prior ledger entry. No production repair function is invoked for testing. Normal verified users may repair their own missing private setup after release. Existing profiles, account role/state and approval data are never overwritten by that repair.

Rollback: first restore the prior application commit if necessary. Preserve any newly initialized private profiles. If the function itself must be rolled back, add a separately reviewed compensating migration restoring the captured prior definition and identical execution grants; do not delete the new migration history or remove profiles. Restoring the old function reintroduces the demonstrated reserved-link failure, so a forward repair is preferred. Historical migration replay is not part of deployment.

## Implemented entry paths and evidence limits

The shared server-only repair helper is called by verified password login, verified session confirmation (`PUT /api/auth`) and authenticated `GET /api/account`. The existing email callback's missing-account provisioner is unchanged; a callback session with an existing account and missing child is repaired when its verified account is loaded. Public role hints never override an established database role or restore an inactive account, and missing privileged accounts require support. Ordinary reads fail explicitly; only confirmed absence can trigger the atomic repair. Strict boolean acknowledgment and a fresh owned account/profile read are required before reporting setup success. There is no automatic write retry, legacy multi-write fallback or compensating Auth deletion.

Nineteen native PostgreSQL cases run the captured production bootstrap/link functions, table columns and thirteen constraints with the new migration. They also reproduce the old reserved-link failure. Twenty-seven helper and actual API-entry cases use that database behind a Supabase-shaped adapter to test repair, preservation, uncertainty and failures. The complete focused auth/account/provisioning set passed 325 cases. Separate delivery-wrapper tests passed transaction preservation, repeated-application rejection and changed-function rejection. The migration's normalized SHA-256 is `4d7cf6a56c61b11554f866ef724c75f2f6cef93cf69864876a2eedc1e22d00e2`; its expected PostgreSQL function fingerprint is `4976257073739281a44a64f8a33324f7`.

The fixture models the Auth user columns needed by bootstrap, the real account/profile/link tables and their insertion triggers. It does not run hosted GoTrue, email-confirmation/notification triggers, multiple independent database connections or email delivery. Those limits are explicit: native rollback coverage is not a real hosted signup, and queued PGlite requests are not proof of multi-connection concurrency. The active missing dancer profile remains a production aggregate observation, not a record altered by testing.

Final Step 7 validation on `9a36b8e6` passed all 3,792 tests without failures, skips or cancellations, full lint, production build, standalone TypeScript and all thirty live readiness checks. This includes the concurrent storage-audit and provider-invoice reconciliation changes. The complete suite used three workers after the standard generators; postbuild skipped layout-review population. All 325 focused entry/provisioning cases and the synthetic delivery-wrapper preservation/repeat/drift checks passed. Read-only production preflight at 02:02:02 UTC on 2026-09-10 confirmed the inspected old function, service-only access, four retained RLS protections and no target ledger entry; there were 29 Auth users/accounts, five customer profiles, sixteen dancer profiles and four aliases. No production row or repair invocation was used for testing. Applying the exact committed migration, verifying its data/access preservation, exact Vercel success and post-deployment health remain the final release gates.
