# Step 9: activate atomic redemption activity

The additive migration was pushed as `89e9e46b14b9a59a3a16a845362a6c348a1edd17`. Its [exact Vercel deployment](https://vercel.com/ai-movie-jobs/shiftstage/GoezaKQNCk64azYkbJfaMmytaD5a) succeeded; production health, event input denials, protected account/finance denials and thirty readiness checks passed at 04:17:35–36 UTC on 2026-09-10.

Guarded production application of the exact committed SQL preserved 36 redemptions, 35 events, two revenue entries, one commission, 128 catalog column rows, 53 constraints, 24 indexes, nine policies, ten triggers, four relation access records, 115 existing public functions and 97 previous migration entries. The 04:15:26 UTC read-only postflight confirmed the expected function definition, invoker access, empty search path, three-second lock timeout and service-only execution. Ledger SQL MD5 is `7f0dfac8d5ee113723ff69eb00cc15df`; migration SHA-256 is `83696ad4b3ee6491af025bfb0c43605a8a398488d8a43c43a7cd640121afc9fd`. Freeze that applied file without changing or reapplying it.

Replace the caller's separate redemption read, timestamp update and event insert with one `record_deal_lifecycle_event_safely` RPC. Preserve token/event inputs, optional verified actor/session and existing request audit fields. A genuine null means no matching token; an RPC error or malformed acknowledgment must fail safely. Return only the expected public result fields. Never fall back to separate writes or automatically retry an uncertain event call.

Test the actual application helper against the installed PostgreSQL migration using synthetic records, including successful guest/authenticated activity, missing tokens, malformed/error results, commit-with-lost-response and rollback. Existing API validation, rate limits, auth-failure handling and non-paying event behavior must remain covered. Repeated deliberate actions retain existing analytics semantics; this release does not add a request idempotency contract.

Run full tests/lint/build/TypeScript/readiness, inspect/stage only this caller/test/documentation and applied-history protection, commit/push and verify exact Vercel success plus safe production health. Do not call the new RPC with real production tokens for validation. If application behavior regresses, roll back the caller release while retaining the additive function and ledger history; investigate before any database change.

The focused set passes 67 tests, including 21 new application/endpoint cases backed by the actual local SQL function. All 21 new cases fail against the old helper. The timeout response correctly follows the existing API policy (503), so the test expectation was corrected without changing that policy. Schema-only production verification at 04:22:48 UTC confirms the server API advertises the new function without invoking it.

The initial combined checks passed 4,195 tests, lint, build, standalone TypeScript and thirty readiness checks. Before committing, incoming `01987783` startup-performance work was inspected and preserved by fast-forward. Its pinned build dependency was installed without changing package manifests/lockfile. Final full validation on this integrated tree remains required before delivery.

Final integrated validation passed all 4,200 tests, lint, production build, standalone TypeScript and thirty live readiness checks. This supersedes the earlier total. Postbuild skipped layout-review population; no new SQL was applied or production event recorded. Exact push, Vercel success and production health remain required.
