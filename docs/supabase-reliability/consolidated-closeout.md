# Consolidated Supabase reliability closeout

On September 12, 2026 the user chose to consolidate the remaining stages. This combines the final permission correction, two confirmed copyright-operation recovery issues, existing critical regression checks and the remaining operational reviews. It does not require separate application releases for environment, observability, regression inventory, backup readiness or the final report.

## Implementation scope

- Retire browser and PUBLIC insert/update/delete access to copyright cases, counter-notices and strikes. Retire service case update/delete and service counter-notice/strike insert/update/delete, including explicit column insert/update grants. Preserve service claimant-notice inserts and existing read/history access. Other SQL privileges, including retained service truncate/reference/trigger permissions, are not claimed to be removed.
- Remove non-owner execution of the obsolete venue-ownership claim reviewer. The current review endpoint already returns HTTP 410; current venue request/credential flows and claim history remain. An unused exported JavaScript helper still exists; this change does not claim it was deleted.
- Check the full returned contact-settings receipt before claiming a save or creating its audit entry. A confirmed save with an uncertain audit receipt returns the saved values plus an explicit audit-review warning. An uncertain save returns 503 and requests a read review; neither path automatically repeats a write. Saved contact details can be reloaded explicitly.
- After an uncertain case action, retrieve that exact case through the authorized administrator read endpoint, including terminal states outside the bounded active list. A case-ID lookup also makes completed cases retrievable after reopening the panel. Preserve the displayed warning, reviewer notes and unsaved contact form. Refresh and lookup repeat only the read; they do not repeat the action or email.

The permission guard uses a freshly captured complete migration ledger and catalog fingerprint, exact current target permissions, six delivered native functions and three ownership attachments. It checks catalog and record preservation inside the same bounded transaction, verifies the intended permission effects and records the exact source once. Native fixtures use directly captured current function definitions and complete table/column permissions; the retired claim fixture preserves all nineteen formatted column types, eleven constraints, five indexes and two policies. Historical foundation replay is not substituted for current definitions.

The existing native restoration/case/forwarding transactions remain authoritative. The contact-settings save and audit remain separate operations with explicit partial-result handling; this release does not claim they became one database transaction. Email delivery has checked receipts and one read reconciliation after uncertain forwarding, but no durable outbox or exactly-once guarantee.

Before integration, all 62 permission/guard cases passed at 18:03:24 UTC and all 38 actual service/HTTP/UI recovery cases passed at 18:07:25 UTC. The same recovery tests against unchanged preceding application sources produced eight passes and thirty failures at 18:11:45 UTC, confirming the targeted gaps. All runs ended naturally with stable source/dependency hashes. Fresh read-only production preconditions at 18:11:59 UTC matched all thirteen current tables, 217 formatted columns, nineteen retired-claim columns, full permissions, the six native functions, three ownership attachments and 129 ledger entries. These focused results precede the required integrated full-suite validation.

## Consolidated review

| Former stage | Evidence and retained boundary |
| --- | --- |
| 18: environment configuration | Existing server/public configuration tests cover privileged/public key separation, malformed configuration and cross-project mismatch. Canonical dependency, runtime, type, lint and production-build checks remain mandatory. The configured production artifact scan checks for private values. No credential value is included in this report. |
| 19: failure resilience | The two confirmed copyright recovery gaps above join the previously delivered native lifecycle ownership and checked caller corrections. Native and actual caller tests exercise rollback, stale versions, uncertain receipts and post-commit delivery failures. Broader provider coordination, deletion/cleanup races and independent hosted concurrency are not certified by these local checks. |
| 20: observability | Safe structured error categories distinguish unconfirmed settings saves from unconfirmed audit entries and case delivery. Architecture release `d999a16218f8db7aacc3663206f36fc8e662b934` separately corrected unavailable monitoring results and reached exact Vercel success. Final health/access and readiness results are verified again for this release. No production business action is invoked as a test. |
| 21: database regression | Existing current-schema PostgreSQL, guarded-deployment and actual-caller suites are retained. The permission cases prove denied direct writes while current native workflows still function; the new recovery cases prove truthful responses without repeated writes. A complete suite run is required after integration, not only focused tests. |
| 22: backup/recovery readiness | A read-only Dancr dashboard observation at 17:14:30 UTC on September 12 found eight daily physical snapshots dated September 5–12, latest September 12 at 10:08:02 UTC. Point-in-time recovery was not enabled. The dashboard states Storage API objects are excluded; a database restore does not recover subsequently deleted objects. No backup was downloaded, restore invoked, setting changed or recovery rehearsal performed. Available snapshots are not a restoration-success guarantee. |
| 23: critical end-to-end checks | Actual service/HTTP/UI tests use synthetic boundaries and current native fixtures. Deployed health and unauthorized-access checks are read-only. Real hosted accounts/mail, independent authenticated browser sessions, independent database connections and a disposable full workflow environment remain user-deferred. Local tests are not described as full hosted end-to-end certification. |
| 24: final classification | Record the exact pushed commit, Vercel success, full gate results, exact SQL receipt and independent preservation checks in the final delivery report. Classify delivered fixes, separate-task work and deferred exercises explicitly. |

## Earlier delivered evidence and qualification

The native foundation closing commit `d4e472700d6dc5651eb048148b7029b13eb98bce` and checked caller commit `6f1a9048139a5c01125f3b11046feb0c21b1e3bf` both reached exact successful deployments. The checked caller release passed 8,816 tests, all eight canonical gates, configured artifact inspection, postbuild TypeScript, 58 readiness checks and ten deployed health/access checks.

Its preservation result was all 85 table row counts and 84 unchanged table fingerprints. `request_rate_limit_buckets` had one changed aggregate fingerprint, independently investigated as unattributed operational TV request-counter activity. All captured metadata, native effects and 129 migration entries matched. The before-capture aggregates cannot retrospectively identify every changed field or originating request. No counters were reset, no caller SQL was applied, and the original failed strict check was preserved. This report does not describe all 85 fingerprints as unchanged.

## Explicit deferrals and separate ownership

Disposable historical migration replay/reconciliation, hosted account and mail exercises, independent connection races and a restore rehearsal remain deferred. No production reset, historical migration replay, account mutation, media deletion or restore is used to fill that evidence gap. This closeout makes no claim about contractual recovery objectives or a rehearsed recovery time.

The separate security and architecture tasks retain media publication/claim fencing, TV completion fencing, the confirmed OneSignal notification correction and copyright monitoring-count alignment under their own users' instructions. The monitoring review found an obsolete `information_requested` filter where the lifecycle uses `needs_information`; the architecture task owns that correction and active-queue count review. Their incomplete work is not marked complete here, and this consolidation does not cancel those tasks. Other lifecycle/provider handoff notes remain disclosed review limits rather than a promise that every cross-service workflow is now atomic.

The proposed permission migration is `supabase/migrations/20260912181237_retire_legacy_case_and_venue_claim_writes.sql`, SHA-256 `f89fd03c2ef250ac87d94f8f0a0b13402ab5dfba2721b62dfb800d80c216a9cc`, MD5 `0c6bc564e7cc3cb6f696e89610b3d050`. Its exact source is frozen in the 172-file history manifest before application; this source protection does not claim a live ledger entry. Integration parent: `8e01369bda91831d0f3952062082b21f67899a0d`. Full validation, task-only commit/push, exact hosting, one guarded SQL application and independent preservation/health remain the delivery gates. Their final results belong in the external closeout delivery receipt and user-facing report; no extra application deployment is needed merely to repeat those results.
