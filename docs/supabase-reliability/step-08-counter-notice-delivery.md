# Step 8: counter-notice delivery acknowledgments

Finance-cron handling was pushed as `62c25da0ecd2603fee6cb67b1fa3ea40cacfd995`; its [exact Vercel deployment](https://vercel.com/ai-movie-jobs/shiftstage/9cPSziFpYMtoiq7kHB9iQ2jc6RvS) succeeded. Both health routes, protected finance denials and thirty readiness checks passed at 03:46:40–42 UTC on 2026-09-10. No authorized finance run was used for verification.

The counter-notice submission path sends email after saving the notice/case, ignores the delivery-status update result and returns `forwarded` based only on the email provider acknowledgment. Its in-app notification also ignores returned errors. The scheduled forwarding path checks update errors but accepts zero affected rows as forwarded. A concurrent completed/rejected notice can be overwritten by the unguarded submission update.

Use one shared delivery-record helper for both callers: condition the update on the exact notice/case still submitted with no forwarding timestamp, require an exact returned forwarded row, and allow one read-only confirmation after an uncertain response. Never resend mail inside that recovery. If confirmation fails, preserve the submitted notice, return an explicit delivery-review warning to the submitting user, and fail the scheduled caller. The existing form already displays the API message and disables duplicate submission once the case is countered. Optional in-app notification failures need sanitized diagnostics without undoing receipt of the notice.

This is a query-acknowledgment correction. Keep legal declarations, existing dates, restoration RPCs, ownership checks, schedule, email content and delivery provider configuration. Do not send real notices/email or alter production cases. The earlier multi-write submission/compensation and durable email dispatch/retry races require Step 9 transaction work; this change does not claim to resolve them.

Test actual library and route behavior using synthetic database/email responses, including committed-but-lost acknowledgments, no matching row, stale terminal states, query errors and notification failures. Read-only production metadata/counts must remain unchanged. Run the complete suite, lint, build, standalone TypeScript and readiness, then commit/push and verify exact Vercel success and safe production health.

All 47 focused cases pass, including 38 new runtime cases; 36 new cases fail against the prior implementation. Both entry points use the same guarded write and at most one recovery read. A failed email is never marked forwarded. A committed update with a lost/empty response can be confirmed without resending. Wrong case IDs, absent/invalid timestamps and rejected/withdrawn/completed notices do not pass confirmation. Optional returned/thrown notification errors preserve the saved receipt and log only safe error metadata. The real API route returns 201 for the completed submission and exposes the delivery-review warning, which the existing form renders outside the disabled submission form.

The read-only production snapshot at 03:49:47 UTC on 2026-09-10 found zero cases, counters and strikes. Its column and constraint catalog was captured without personal records. Runtime fault tests use synthetic database and email boundaries; they do not claim hosted SMTP delivery or multi-connection concurrency verification.

Final validation passed all 4,111 automated tests, full lint, production build, standalone TypeScript and thirty live readiness checks. Postbuild skipped layout-review population. The 03:56:27 UTC read-only capture matches all prior counts/states, 59 columns and 26 constraints. No migration, production write or real delivery occurred. Exact push, Vercel success and production health are the remaining release gates.

The initial push was rejected because `bbcae094` (partial payout-reversal protection) reached main. Rebase preserved that independent correction without conflict. Final validation on the combined tree passed all 4,141 tests, lint, production build, standalone TypeScript and thirty live readiness checks. This supersedes the earlier total. No force push is used.
