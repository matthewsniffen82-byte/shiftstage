# Step 9: activate atomic NFC support requests

Prerequisite `1cd620d9cb03f78aa15a6384ea78692a99618ee3` is pushed with matching main references and [successful exact Vercel deployment](https://vercel.com/ai-movie-jobs/shiftstage/3vTPKYPhTQiW1w1PgMEHvXHAPjsi). Read-only verification at 04:50:34 UTC confirms the new function MD5 `8819ae94a588addf7d76f48f7500643b`, ledger source MD5 `a119552013b1c9c741dc9daeb803219f`, service-only invoker execution and unchanged nested helper. The guarded transaction preserved 29 users, 22 venues, twenty tags, zero team memberships/requests, seven threads, nine messages, 78 notifications, 157 catalog column rows, 46 constraints, 27 indexes, nineteen policies, two triggers, eight relation access records, 116 existing public functions and 98 previous migrations. Production health, NFC GET/POST authorization denials, protected account/finance routes and thirty readiness checks passed at 04:52:41–44 UTC. No production support message was sent.

## Narrow implementation plan

Freeze that exact migration without reapplying it. Replace the NFC endpoint's separate request insert, message call and compensating request deletion with one checked atomic RPC. Retain bounded request parsing, authenticated venue access, scoped tag lookup, sessions, no-store responses and existing optional external delivery/activity. Validate returned identity, ownership, type, notes, status and timestamp before claiming success. Never fall back to separate writes or automatically retry an uncertain transaction. Preserve the existing send rate limit and return sanitized authorization, validation, missing-tag and temporary-failure errors.

The form will retain one UUID for unchanged submission content while mounted, reuse it for a user retry after a transient failure, and reset it after confirmed success or changed input. Block double submission synchronously and prevent input changes during a submission. This does not promise persistence after reload or deduplication for legacy clients that omit an ID; those receive a fresh server UUID. A completed duplicate returns its existing request without repeating external delivery or optional activity. Optional delivery/activity failures must not misreport a committed support request as failed.

Test the actual helper/endpoint against the deployed SQL fixture, including response loss followed by same-key retry, conflicting identities, rollback, failed/malformed acknowledgments, denied access, preserved fields, server-only caller and optional-effect failures. Exercise the actual form handler with deferred and failed responses. Run full suite, lint, build, standalone TypeScript, migration guard and read-only readiness. Push the single caller commit, verify exact Vercel and production health before the next workflow. Rollback is the previous application deployment; the additive unused function may remain. No production support requests, emails or account mutations are needed.

## Remaining delivery boundary

In-app notification rows commit with the request and message. External push/email delivery and the optional venue activity log remain best effort. A response lost before external delivery can leave those optional effects unattempted; replay deliberately does not resend them. Durable external dispatch/reconciliation is a separate audit concern. The request and support inbox remain the source of truth. Cancelling closes the pending form identity; use the unchanged form's retry to recover an uncertain submission.

## Validation before delivery

All 57 focused checks passed, including 44 new tests of the actual endpoint/helper and form handler. Running the endpoint tests against the prior route produced 35 failures, confirming the tests detect the old separate-write path. Full validation passed all 4,302 tests, lint, production build, standalone TypeScript, migration-history guard and thirty live readiness checks. Postbuild skipped layout-review population. The read-only server API schema check at 04:59:38 UTC confirms RPC exposure; the 04:59:40 UTC database check confirms frozen function/ledger hashes, server-only privileges, RLS and unchanged support counts. No migration was reapplied. Exact push, Vercel success and deployed health are still required.
