# Step 15 — consistent active NFC sticker capacity

The existing provisioner checks a maximum of 25 active stickers per venue without serializing its count/insert. Enabling a disabled sticker and rotating a disabled sticker have no capacity check. The actual schema/RLS native fixture reproduces 26 active stickers through both existing administrator RPCs. Provisioning, enable and rotation each remain atomic with their existing administrator audit; the gap is consistent admission, not partial existing transactions.

The September 12 05:40:27 UTC read-only preflight found 20 active tags, no disabled/revoked tags, a maximum of three per venue and zero venues over capacity. All scan counts are nonnegative. No sticker, secret, scan, enrollment, venue or audit entry was changed by the investigation.

## Correction and caller compatibility

Add one invoker trigger function and one AFTER INSERT/UPDATE OF venue_id,status attachment. A new active slot takes a common transaction advisory lock for the destination venue, then counts active tags in a separate query. More than 25 raises the existing capacity error. All three current administrator RPCs remain unchanged; the table guard also covers direct service writes and authorized administrator writes through the existing RLS policies.

An already-active tag staying in the same venue consumes no additional slot. Disabling/revoking releases capacity. Rotation of an active sticker can replace it at capacity; rotation of a disabled sticker must acquire an available slot. A failed replacement, conflicting digest, full destination or mandatory audit failure rolls back the original tag's revocation, replacement and audit together. Existing rotation history and tokens remain untouched by deployment. Ignored duplicate inserts and updates to an existing occupied slot remain possible. Any hypothetical pre-existing excess rows are retained while further expansion is blocked.

After-trigger timing observes the final row and ignores rows that `ON CONFLICT DO NOTHING` does not insert. Read Committed takes a fresh snapshot after waiting for the lock; Serializable uses PostgreSQL serialization checks. Repeatable Read new-slot operations are rejected because the earlier snapshot can conceal another reservation. Native tests establish held-lock identity, per-venue separation and commit/rollback release. They do not replace the still-deferred independent hosted-session concurrency tests. PostgreSQL describes [trigger transaction behavior](https://www.postgresql.org/docs/17/trigger-definition.html) and [data visibility inside procedural functions](https://www.postgresql.org/docs/17/spi-visibility.html).

Preserve the actual NFC table permissions: authenticated accounts have table write grants, while RLS permits administrators to manage tags and venue owners to read their assigned tags. An active administrator sees all tags needed for the count; ordinary/inactive accounts cannot insert or change a tag. The new function receives no PUBLIC, anonymous, authenticated or service-role EXECUTE grant. PostgreSQL invokes the installed trigger during authorized table writes. No existing grant or policy changes; the current `is_admin()` helper's default PUBLIC execution ACL remains as captured and already allowed by the function-security gate.

## Native evidence and production application

The fixture contains both complete target tables: 23 columns, twelve constraints, six indexes, all three current policies, the three exact administrator functions and actual administrator predicate. Account/venue control relations and the auth claim accessor are documented synthetic projections. The active-label unique indexes remain installed, alongside token uniqueness and rotation-reference constraints. Full browser read-policy coverage remains in the separately maintained access matrix.

All fifty native cases pass. They cover the two reproduced old failures, capacity through the three existing RPCs, direct service/administrator writes, individual/multirow activation, cross-venue movement, last-slot behavior, active/disabled rotation history, duplicate retries, token conflicts, null/domain errors, revoked tags, role denial, mandatory audit failure and lock lifetime. Twenty guarded-deployment cases pass for schema/permission/dependency drift, injected row or unrelated metadata changes, preservation and repeat rejection.

The guard checks current target columns, constraints, indexes, policies, grants and attachments plus the exact four dependency definitions/ACLs. It uses bounded locks on the migration ledger and tag table, preserves twenty relevant tables through in-transaction counts/fingerprints, and verifies other functions/schema/access/roles/prior ledger rows remain unchanged. Its catalog capture reuses the already tested Step 14 metadata query with only this newly added function/attachment excluded; Step 14's video function and trigger are included in preservation. The final deployment is generated from the exact committed SQL and records that source in the migration ledger. Vercel does not apply database migrations.

The production read-only guard preconditions passed at 05:54:30 UTC. Reviewed normalized source SHA-256 is `2a16569db2917e4801e85718f2349b9ab8e34b11e92d3ee178164f66413419a1`, MD5 `be42c9234a262d649d2cdca5592dbc20`; expected new function fingerprint is `93ad88652a30d0a5d3bfdf50b0b1bd6a`. The table lock can briefly delay sticker changes; a three-second lock timeout and thirty-second statement timeout abort the release on contention. Recovery is a reviewed forward removal of this new attachment/function, which restores the capacity gap without rewriting records or historical ledger entries.

Freeze the previously applied Step 14 migration in the immutable-source manifest only after matching its committed hash, live ledger hash, deployed function fingerprint and attachment. Preserve all prior manifest entries and collision disclosures; historical replay remains deferred. Do not freeze this NFC migration as already applied before its own delivery is verified. After exact application and preservation verification, add its verified hash in a closing manifest/receipt commit, rerun the full release gates and verify that exact deployment before handing back the shared workspace. This keeps the applied-source protection window inside this controlled release.

This is the first Step 15 correction, not the entire data-validation closure. Agent hierarchy concurrency and remaining domain/constraint validation continue after delivery. The retained `club_deals_liquor_free_check` still has one inactive historical exception and zero active exceptions; do not remove the constraint, force validation, activate the row or rewrite history. Full combined validation, commit/push, exact committed SQL application, Vercel and independent preservation/health remain required before publishing this correction or advancing to Step 16.

The integrated correction passed all 6,601 tests with zero failures, skips or cancellations, full lint, the production build with population skipped, standalone TypeScript after the build and thirty read-only readiness checks. Exact commit/push, guarded committed-source SQL application, deployment verification and the subsequent manifest closing release remain required.

## Verified correction and source protection

Correction commit `c5d3ddc91178c81df1c884050d401682b81603eb` reached [exact Vercel success](https://vercel.com/ai-movie-jobs/shiftstage/AoyhjPbTF8sU5hFLrKeLqYccrJjR). The guarded deployment applied the exact committed SQL and recorded the reviewed source hash. Independent checks at 06:26:37 UTC on September 12 preserved all 131 previous functions, 118 prior ledger entries, ten schema groups and twenty table fingerprints, including twenty NFC tags, 34 videos and 452 storage objects. The new function fingerprint, invoker/owner/path/ACL and enabled attachment matched; the catalog now contains 132 functions, 61 trigger attachments and 119 migration entries. The function-security gate passed. Seven deployed health/access checks and thirty readiness checks passed at 06:27:52 UTC; clean local, tracking and remote main matched the correction commit.

The closing release freezes the verified NFC source, extending the manifest from 160 to 161 entries without changing prior metadata or hashes. Three native history cases exercise the actual verification code: each deployed capacity migration passes unchanged and rejects a synthetic later SQL alteration. The preceding 159-entry manifest admits each same alteration, demonstrating the protection added for the video and NFC releases. The cohort is pinned to these historical versions so later additive migrations do not invalidate the regression.

No additional SQL or business-data changes are part of the closing release. Its full combined release gates and exact deployment verification remain required. Step 15 remains open for agent hierarchy and remaining domain validation after this NFC subrelease.

The closing candidate passed all 6,604 tests with zero failures, skips or cancellations, full lint, population-disabled production build, standalone TypeScript after the build and thirty readiness checks. Final commit/push, exact Vercel, fresh production health and unchanged database verification remain required before handoff.
