# Step 17: preserve independent lifecycle decisions during copyright enforcement

## Problem and resulting behavior

The captured takedown and restoration functions reproduced six errors: a later video decision could be overwritten; one case could republish a video still held by another case; resolving an earlier case could leave a repeat-strike suspension in place below three strikes; a fourth case could replace the original suspension snapshot; restoration could overwrite later account or profile restrictions; and a previously hidden video could acquire a manufactured submitted state.

The correction records a private, bounded previous/applied state for each affected account, profile or video. Repeated takedowns retain the first still-owned snapshot. Explicit later decisions, including same-value writes to controlled columns, invalidate permission to restore that state. Unrelated caption or display-name changes retain the snapshot and their edited values. Restoration preserves other active cases, repeat-strike restrictions, administrative restrictions, deleted accounts and independently changed publication state. It restores the actual previous video status, publication time and review notes only while the copyright action still owns that decision.

The existing case, counter-notice and strike records remain authoritative for eligibility. A restoration requires the finite waiting period to have elapsed, a valid forwarded counter-notice, matching identity, no court filing and an eligible case state. It rescinds exactly the case's owned strike. Crossing below three active strikes can release still-owned account/profile enforcement regardless of which earlier case triggered the suspension. Required case, strike, content, notification and audit writes commit together; suppressed required writes fail and roll back the entire action. Physically deleted accounts remain deleted while their nullable-uploader case history can be resolved without recreating content.

## Native foundation and compatible application rollout

Migration `20260912140704_preserve_owned_dmca_lifecycle_states.sql` creates the private `dmca_enforcement_states` table, one invalidation helper and three AFTER UPDATE/DELETE attachments. It replaces takedown, restoration and counter-notice submission with checked transactional writers. It adds two initially unused service-only functions: an administrator case transition requiring the exact expected status/time, and a counter forwarding receipt that records only previously acknowledged email delivery. All six functions use empty search paths; the five business writers require the service boundary, and direct invalidation-helper execution is denied. Business writers use bounded locks and explicit transaction assumptions. No SQL function sends email or changes Auth-provider metadata.

The application refinement is compatible with the prior and new restoration receipts. It validates optional outcome/content-state fields when present, returns only checked fields, and describes copyright resolution without promising publication. Existing confirmed-action/email-failure separation remains. Deploy this compatible source successfully before applying its exact SQL. Administrator action and forwarding callers are deliberately connected in a later release; direct legacy case/counter/strike write access is retired only after those callers are deployed. Publication fallback retirement is also a separate caller change.

The exact proposed SQL has normalized SHA-256 `e5e2fdea66eeaebdaa9c6230faaacf83b34181020dd36d1979100cd34c50364b` and MD5 `c982ee4809ff0ea544762e4fbe8ce7b5`. These identify source, not proof that it has been applied. Native SQL components are checked against this complete source; behavior and guarded deployment tests execute those same components.

## Combined evidence and deployment safeguards

The independent read-only capture at 13:55 UTC on September 12 includes the final Security 27 account pause/recovery implementation. The native fixture reconstructs twelve full tables, 209 columns, ninety constraints, 44 indexes, 25 policies, 21 actual functions and twelve source attachments. Historical dropped column positions, function ACLs and all full-target raw/effective table and column permissions are asserted exactly. The only observed PostgreSQL 17/18 constraint rendering difference is the separately asserted equivalent parentheses in the video dimensions CHECK; no general schema mismatch is waived.

All 426 external candidate checks passed in one combined run, including the later caller and permission candidates. The adopted foundation contributes 262 native/guard/caller checks. Fifteen combined account/copyright cases cover self-pause followed by repeat enforcement, restricted pause/resume attempts, actual administrator profile decisions, same-value operator decisions, unrelated edits, failure rollback of both private stores, self-deletion, synthetic physical Auth-row cascading, unrelated venue pauses and ineligible restoration. Deliberate operator setup writes run as the fixture operator; current business RPCs and direct-write denial checks retain their actual service role.

The production wrapper requires exact target schemas, raw/effective ACLs, captured dependency bodies/owners/grants, absent new objects and an unchanged eligible ledger boundary. Existing copyright cases, counters, strikes or account/profile copyright markers reject application and require a separate transition plan. It locks target/ledger relations with a three-second lock and thirty-second statement limit, verifies exact new effects, preserves unrelated metadata and every selected table fingerprint, and records the exact source once. Native tests cover drift, rollback, permission leaks, a 93-table preservation scope, real outer COMMIT, temporary-capture cleanup and replay rejection. No production business RPC is used for verification.

## Limits and remaining Step 17 work

A prior self-pause permission is invalidated by a later account enforcement decision. Restoring an earlier disabled account state does not recreate self-resume permission; separate authorized operator recovery may be needed, while publication remains restricted. Public media URLs can outlive application visibility. The active administrator case list does not establish complete terminal-history or email reconciliation; Step 19 owns that work. Architecture Step 13 owns the stale image-worker claim fence.

Auth identity/metadata and peripheral gallery/shift dependencies are explicit projections. PGlite provides a single local connection with synthetic records. Hosted independent-session contention, real account/mail, historical replay and restore exercises remain deferred by the user. No production deletion, storage cleanup, email or account recovery is performed as a test. Recovery from a deployment problem uses a reviewed forward fix or disables the affected action; retain private ownership records and immutable history. A rollback must retain truthful resolution messages and must not reintroduce weaker direct-write fallbacks.

## Verified native implementation and source protection

Implementation `8916d27ec528876cdb649fbaa5684705c390a2f9` reached [exact Vercel success](https://vercel.com/ai-movie-jobs/shiftstage/96QUTNkJLYax1jFvo8qraspGcqfo). All 8,032 tests, all eight canonical gates, configured production artifact inspection, postbuild TypeScript and 41 readiness checks passed. The exact committed migration applied once. Independent verification at 2026-09-12T14:38:27.465Z preserved 84 table fingerprints, 154 unrelated functions, 69 prior trigger attachments and 126 prior ledger entries, verified six native function effects and three new attachments, and recorded ledger entry 127. Ten deployed health/access checks passed; local, tracking and remote main matched, and both unrelated screenshots were preserved.

This closing change freezes the verified SQL in the 169-source history manifest, preserving every previous hash and the original capture metadata. It adds ten read-only readiness checks for the copyright schema, five business interfaces and denied browser access to private ownership records. It applies no further SQL or business mutation. Its complete validation and exact deployment are verified separately in the delivery receipt. Later administrator/forwarding callers, publication fallback and legacy-write retirement remain within Step 17; Steps 18–24 retain their ordered releases and existing hosted-exercise deferrals.
