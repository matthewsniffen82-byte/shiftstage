# Step 26 — sensitive data minimization

This step started after Step 25's exact commit `e947a5ed57d2c08cd2bd0faac95b35b13fd43855` deployed successfully and passed its live checks at 2026-09-12T09:51:20.964Z. Preparation uses an isolated copy while the Supabase and architecture releases own the shared workspace. The final candidate includes the separately delivered Supabase Step 16 commit `de1f3762e491ba5aae5d28354476933312df31d0` and Arch Step 8 commit `19ca1b4292af071a3d6395bb1f044acd4d367d03`.

## Findings and changes

**MEDIUM — internal deal properties cross public response boundaries.** The shared `ClubDeal` value includes referral fee type/amount/currency and arbitrary internal redemption rules. Public discovery, TV enrichment, NFC lookup and completion returned that value directly. Dancer and campaign pages also passed it as props into client components. The customer components do not use these financial properties, whereas authorized venue/admin screens do. A server-rendered page is still a disclosure boundary when its props reach a client component. [Next.js server and client components](https://nextjs.org/docs/app/getting-started/server-and-client-components).

An explicit twelve-field `PublicClubDeal` projection now preserves offer identity, customer copy, availability, booking destination and ordering. All six affected response/props paths select that projection. Attribution tokens, offer selection, the privileged redemption result and guarded venue/admin financial tools keep their existing behavior. Unknown future private fields are excluded by construction. Tests execute the actual HTTP handlers, TV enrichment function and page functions using synthetic service responses; no real NFC scan, redemption or payment is issued.

The fresh neighboring release's 2026-09-12T10:01Z metadata confirms `club_deals` still grants table SELECT to both browser roles. Therefore an HTTP-only fix would leave direct REST access. The new bounded migration removes table SELECT and old column SELECT grants from PUBLIC/anon/authenticated, then grants exactly twelve customer-facing columns to anon/authenticated. Seven internal columns, including three lifecycle timestamps, become unavailable for direct selection or filtering. RLS, service access, unrelated grants and data are preserved. Effective inherited access is checked, with rollback if it survives. [PostgreSQL privileges](https://www.postgresql.org/docs/current/ddl-priv.html).

The guarded deployment compares the current target catalog, preserves all other captured metadata, fingerprints deal rows within the transaction, checks the effective public projection and records only the exact committed migration. It does not replay historical SQL, validate the existing inactive liquor-offer exception, invoke financial functions or rewrite business records. The owned read-only production preflight at 2026-09-12T10:24:51.642792Z confirms all nineteen columns are currently selectable by both browser roles, no deal-referencing public view and one browser-callable boolean predicate. It captures 42 rows by count and fingerprint only. Exact committed application and postflight remain required.

**LOW — redundant personal information persists after its immediate use.** The homepage stored per-role emails independently of the active session, and could display a previous email after sign-out. It also kept up to 500 administrator review notes in local storage indefinitely. Temporary signup/reset resume records had no expiry or collection bound when unused.

Email displays now use only the current signed-in account of the requested role. Homepage and dedicated account-session reads/clears remove the exact retired email/review-cache prefixes while preserving unrelated storage. Optimistic review notes stay in the current page, are scoped to the admin account and clear on account changes; the server remains the review record. Same-account token refresh retains immediate feedback. Resume records expire after 24 hours, retain at most twenty recent entries, select only known navigation fields and remove malformed/oversized caches. Valid confirmation and reset navigation remains available. Existing token/session architecture is retained.

## Review coverage and retained data

| Surface | Result |
| --- | --- |
| Public profiles, venue discovery, photos/social links and TV | Existing explicit mappers exclude legal names, account ownership, contact email, private moderation/storage records and precise device proximity. This step fixes the separately traced deal payload. Profile eligibility, schedule/location precision and visibility failover are Step 27. |
| Direct database views/functions | The reviewed metadata contains no deal-referencing public view. The only browser-callable function referencing `club_deals` is the existing boolean venue offer predicate; its hidden-venue visibility behavior remains Step 27. Other financial functions remain server-only under the earlier grants review. |
| Account, support, notifications, DMCA, moderation and financial tools | Recipient/owner/admin controls and purpose-specific data remain. Necessary user/admin-authored case notes and accounting/legal attribution are not deleted speculatively. No production private row or identity document was downloaded. |
| Browser state | Duplicate emails and persistent review notes are removed; unused auth-resume state is bounded. Current authentication credentials, user-requested device deal saves, short-lived NFC navigation and unfinished profile drafts retain their existing functions. |
| Logs and public artifacts | Earlier fixed diagnostic projections and the mandatory production artifact scan remain. Provider retention/readers/drains and previously documented administrative settings are not certified. |

The same read-only aggregate preflight finds **18 legacy verification objects and 7 verification review records**. Their contents were not retrieved. The available bulk-purge command was not executed: a count does not establish retention expiry, legal obligations, references or a recovery source. These records remain a documented retention decision for the operator; this step does not claim they were deleted.

## Evidence and limitations

The first 50 new tests passed: eighteen browser cases, eleven public-response/props cases and twenty-one PostgreSQL privilege cases. The exact pre-change source failed 27 of 29 selected assertions, reproducing response disclosure, retained email/review notes and direct internal-column access. All failures were assertion failures rather than missing fixture dependencies. Two unaffected role-email cases already passed.

Ten additional native PostgreSQL cases test the complete deployment wrapper, catalog drift, row preservation, unauthorized permission changes, inherited access, exact source-ledger recording and replay rejection. Nine later browser cases cover expired/bounded resume state and dedicated account-page cleanup, bringing new coverage to 69 tests. All 124 focused checks pass. Those nine later browser cases also fail on the prior source, giving 36 reproduced pre-change assertion failures in total. Synthetic fixtures do not replace fresh production column ACL verification or disposable authenticated staging tests.

Privacy cannot revoke an already downloaded response, a copied image, a browser backup or an old offline deployment. Cache cleanup takes effect when updated code runs and storage is accessible; a browser that blocks removal cannot be certified cleaned. Private business-record retention periods and operator/provider policy decisions are not invented by this patch. No bulk purge, account creation, paid service or provider-console change is included.

## Delivery

The complete isolated release gate on exact parent `19ca1b4292af071a3d6395bb1f044acd4d367d03` passed at 2026-09-12T10:24:56.481Z: **7,192 tests**, zero failures/skips/cancellations, dependency/signature audits, route type generation, standalone TypeScript, full zero-warning lint and production build. The artifact guard checked 263 files, 239 text assets and 7,715,539 text bytes; postbuild skipped population. All source files are compared before integration, followed by the configured shared build, private-value artifact scan and postbuild TypeScript check. Task-only commit/push, exact committed migration application, exact Vercel success, deployed health and the verified migration-manifest followup remain pending. Step 27 has not begun.


## Step 26 applied database boundary

The implementation was committed and pushed as `3e7eeb4fbfdfec183d74f95099d3814498056882`. Only its committed migration `20260912103000` was applied. The bounded transaction verified unchanged captured metadata and all 42 deal records. The independent read-only postflight at 2026-09-12T10:33:57.847454+00:00 confirms exactly twelve selectable public columns, seven protected columns, unchanged RLS/owner, full service access and the exact source-ledger MD5 `275d0073ca7489e1067a008cb6abec58`. All 32 database readiness checks pass. The normalized SQL SHA-256 is `d3a982374fb011f627e38c301215e639a5f04785ecd2836be8174f124c7c6ef8`. This same-step followup adds only that verified file to the immutable migration manifest (165 files); it replays no SQL. Full followup validation and both exact hosted release checks remain before Step 27.


## Step 26 implementation release and followup validation

Implementation `3e7eeb4fbfdfec183d74f95099d3814498056882` reached [exact Vercel success](https://vercel.com/ai-movie-jobs/shiftstage/CrjLC7RHfyucwiudALvTGPezsjuJ). Eight production health/anonymous-denial checks passed at 2026-09-12T10:40:25.601Z; thirty-two database readiness checks and deployed discovery/TV deal-field projections passed. The homepage rendered without browser warnings or errors. No live NFC scan, redemption, paid/provider action or private row retrieval was used. The checksum followup passed all **7,192 tests**, zero failures/skips/cancellations and all eight canonical gates at 2026-09-12T10:42:46.652Z, plus configured private-value artifact inspection and postbuild TypeScript. Only these two evidence records and the one verified migration-manifest entry change. Its exact commit/deployment closure remains required before Step 27.
